"use client";

/**
 * Full session booth UI (record / retake / plan / preview).
 * Loaded dynamically by project-session-impl when this module exports a real page.
 * Until the full module is restored, the resume router stays on the entry screen.
 */
export default function ProjectSessionUI() {
  return null;
}
